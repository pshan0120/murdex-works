# 텍스트 파일 내 HTML 포맷 규칙

이 문서는 `works/` 아래 모든 작품의 `.txt` 파일에 들어가는 HTML 콘텐츠 블록(단계 설명, 단서의 HTML 필드, 엔딩 내용, 질문 등)의 들여쓰기 규칙을 정한다. 앞으로 만드는 모든 작품에 동일하게 적용한다.

HTML 자체는 공백을 무시하고 렌더링되므로, 이 규칙은 실제 화면 출력과는 무관하다. 순전히 사람이 이 txt 파일을 읽고 편집할 때의 가독성을 위한 것이다 — 들여쓰기가 들쭉날쭉하면 중첩 구조를 한눈에 파악하기 어렵고, 수정할 때 실수하기 쉽다.

## 핵심 원칙: 2칸 들여쓰기, 중첩 레벨마다 +2칸

- 최상위 컨테이너(`<div class="story-content">` 등)는 들여쓰기 0.
- 그 안의 직계 자식(`<div class="section-title">`, `<p>`, `<ul>`, `<li>` 등)은 2칸.
- 그 안에 또 중첩되는 요소가 있다면, 거기서 다시 +2칸씩 늘어난다.

## 규칙 1 — 한 파일 안에서 들여쓰기 스타일을 섞지 않는다

같은 파일 안에서 어떤 태그는 0칸, 어떤 태그는 2칸, 어떤 태그는 4칸으로 제각각이면 안 된다.

**새 내용을 추가하기 전에 반드시 확인**: 수정하려는 파일이 이미 이 표준(2칸 단위)을 따르고 있는지 먼저 본다.
- 이미 따르고 있다면, 새 내용도 그 들여쓰기 레벨에 맞춰 추가한다.
- 파일 자체가 이미 다른 스타일(예: 0칸)로 되어 있었다면, 새 내용만 거기 맞추지 말고 — 파일 전체를 표준으로 먼저 정리한 뒤 새 내용을 추가한다. 기존의 잘못된 스타일을 그대로 따라가면 파일이 점점 더 뒤섞인다.

## 규칙 2 — 여러 줄 `<p>` 처리

문장이 길어서 여러 줄로 나눠 쓸 때는 다음 형태를 따른다:
```
  <p>
    문장 내용이 길어서...
    여러 줄에 걸쳐 이어진다.
  </p>
```
`<p>`와 `</p>`는 같은 들여쓰기(그 레벨의 기본값, 보통 2칸), 안쪽 텍스트는 그보다 한 단계 더 들여쓴다(4칸).

문장이 한 줄에 들어갈 만큼 짧다면 굳이 여러 줄로 쪼개지 않고 `<p>내용</p>` 한 줄로 쓴다. 대사가 이어지는 대본체 장면(단계_N.txt의 대화 부분 등)은 대부분 이 한 줄 형태를 쓴다.

## 체크리스트 (HTML 블록을 새로 쓰거나 수정한 뒤)

1. 새로 추가한 태그의 들여쓰기가 그 파일에서 같은 위치(같은 중첩 레벨)의 기존 태그와 일치하는가?
2. 한 파일 안에 서로 다른 들여쓰기 방식(0칸/2칸/4칸)이 섞여 있지 않은가?
3. 만약 수정 대상 파일 자체가 이미 일관성이 깨져 있었다면, 새로 추가한 부분만 맞추고 넘어가지 않았는가? — 이 경우 파일 전체를 정리해야 한다.
